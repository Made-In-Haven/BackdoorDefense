# Codex 修改记录（2026-03-23）

本文档记录本轮协作中对项目所做的代码、配置与文档修改，便于后续复现、审阅和继续开发。

## 1. 修改目标概览

本轮修改主要围绕 Anchor-based defense 的两阶段流程展开，目标包括：

- 澄清并补充第一阶段、第二阶段的设计文档。
- 修正第二阶段联合训练的实现细节，使其与设计描述一致。
- 统一第一阶段 artifact、第二阶段 checkpoint、单独 anchor artifact 的字段组织方式。
- 为 UCIHAR 数据集补充可直接运行的第一阶段与第二阶段配置文件。
- 在第二阶段训练日志中显式输出纵向联邦主任务损失与训练准确率。
- 调大第二阶段配置中的锚点损失权重。

## 2. 文档修改

### 2.1 补充方案指导大纲

修改文件：

- `纵向联邦后门防御方案实现指导大纲.md`

主要补充内容：

- 新增“第二阶段的正确定义（补充说明）”。
- 明确第二阶段的核心目标、初始化来源、哪些参数继续训练、哪些参数默认冻结。
- 明确第二阶段的前向流程、损失定义、攻击者在第二阶段中的行为边界。
- 明确第二阶段与第三阶段检测的衔接方式。
- 在“联合训练”和“容易踩坑”部分补充实现注意事项。

关键结论：

- 第二阶段回到标准 VFL 联合训练。
- 第一阶段分类头在第二阶段默认冻结，只作为 projector 和 anchor 的来源。
- 第二阶段真正参与优化的是全局模型与各被动方本地模型。

## 3. 第二阶段代码修改

### 3.1 `utils/trainer.py`

修改内容：

- 增加统一的 `trade-off` 计算函数 `_compute_trade_off()`。
- 在启用 anchor defense 时，训练开始会显式打印进入第二阶段的日志。
- 修正训练/测试模式切换问题：
  - 原逻辑中 `test()` 结束后模型停留在 `eval()`；
  - 现已改为每个 epoch 开始时重新将所有模型切回 `train()`。
- 第二阶段训练日志改为显式打印：
  - `VFL loss`：纵向联邦主任务交叉熵损失；
  - `total loss`：总损失；
  - `anchor loss`；
  - `VFL train accuracy`。
- 增加每个 epoch 结束时的第二阶段训练汇总日志。
- 测试阶段返回值中加入 `test_trade_off`，并统一测试与保存最佳模型时的 trade-off 计算口径。

影响：

- 第二阶段训练过程更可观测。
- 日志中可以直接区分主任务损失与总损失。
- 避免模型因未切回 `train()` 而影响后续 epoch 训练行为。

### 3.2 `defense/anchor_defense.py`

修改内容：

- 增加 `ANCHOR_STATE_VERSION`、`ANCHOR_ARTIFACT_VERSION`、artifact 类型标识等元信息。
- 支持在构造 `AnchorDefense` 时传入：
  - `anchor_bank`
  - `detector_state`
- 新增内部辅助方法：
  - `_normalize_anchor_state_payload()`
  - `_extract_head_state_list()`
  - `_load_local_models_from_payload()`
  - `_build_anchor_artifact_payload()`
  - `_build_stage1_artifact_payload()`
- 将第二阶段 checkpoint 中的 `anchor_state` 规范化为统一格式：
  - `state_type`
  - `anchor_state_version`
  - `heads`
  - `anchor_bank`
  - `detector_state`
- 将第一阶段 `stage1_artifact.pt` 的结构统一为：
  - artifact 元信息
  - `local_models`
  - `anchor_state`
- `anchor_bank_path` 对应的单独保存文件现改为“完整 anchor artifact”，而不是只保存 anchor bank tensor。
- `load_from_artifact()` 与 `load_from_checkpoint_state()` 统一走 anchor state 解析流程。
- `load_stage1_artifacts()` 对旧版 artifact 保持兼容：
  - 兼容旧的 `stage1_artifact_version = 2`
  - 兼容旧格式里 `heads` / `anchor_bank` 直接平铺在顶层的情况

影响：

- 第一阶段 artifact、第二阶段 checkpoint、单独 anchor artifact 的组织风格趋于统一。
- 恢复训练与测试时，anchor 相关状态更完整。

### 3.3 `defense/detector.py`

修改内容：

- 为 `MultiClientDetector` 增加：
  - `state_dict()`
  - `load_state_dict()`

影响：

- 检测阈值等 detector 状态可以与第二阶段 checkpoint / anchor artifact 一起保存和恢复。

## 4. `main.py` 相关修改

修改内容：

- 调整日志表述：
  - 原“Saved anchor bank”改为“Saved anchor artifact”。
- 调整参数注释说明：
  - `--anchor_bank_path` 现在明确表示“serialized anchor defense artifact”的路径。

影响：

- 减少“这个路径里到底只保存 anchor bank，还是保存完整 anchor 状态”的歧义。

## 5. 配置文件新增与调整

### 5.1 新增第一阶段配置

新增文件：

- `configs/ucihar_anchor_stage1.json`

用途：

- 专门用于运行第一阶段预训练。
- `mode = "pretrain_anchor"`
- `defense = "anchor"`
- 强制重跑第一阶段：
  - `force_stage1_retrain = true`

输出目录：

- `./results/UCIHAR_anchor_defense_stage1`

### 5.2 新增第二阶段配置

新增文件：

- `configs/ucihar_anchor_stage2.json`

用途：

- 专门用于运行第二阶段联合训练。
- `mode = "train"`
- `defense = "anchor"`
- 默认复用第一阶段产物：
  - `anchor_stage1_dir = "./results/UCIHAR_anchor_defense_stage1/anchor_stage1"`
  - `anchor_bank_path = "./results/UCIHAR_anchor_defense_stage1/anchor_artifact.pt"`

### 5.3 调大第二阶段锚点损失权重

修改文件：

- `configs/ucihar_anchor_stage2.json`

修改项：

- `lambda_anchor: 0.1 -> 1.0`

目的：

- 加强第二阶段对锚点空间的约束力度。

## 6. 当前推荐运行方式

### 6.1 运行第一阶段

```powershell
python main.py --config configs/ucihar_anchor_stage1.json
```

### 6.2 运行第二阶段

```powershell
python main.py --config configs/ucihar_anchor_stage2.json
```

## 7. 本轮涉及的文件清单

- `纵向联邦后门防御方案实现指导大纲.md`
- `defense/detector.py`
- `defense/anchor_defense.py`
- `utils/trainer.py`
- `main.py`
- `configs/ucihar_anchor_stage1.json`
- `configs/ucihar_anchor_stage2.json`

## 8. 已做的检查

本轮已完成的轻量检查包括：

- 对新增/修改的 JSON 配置做了解析检查。
- 对新增/修改的 Python 文件做了 AST 语法检查。

说明：

- 未在当前记录中完成完整训练级验证。
- 当前环境存在 `__pycache__` 写入权限问题，因此未采用依赖 `.pyc` 输出的编译检查方式作为最终验证手段。

## 9. 后续可继续改进的方向

- 增加第三阶段 `test` 专用配置文件。
- 将 `stage1_metrics.json` 进一步纳入统一 artifact 元数据体系。
- 补充 README 中关于第一阶段/第二阶段运行顺序的说明。
- 根据实际训练结果继续调节 `lambda_anchor`、`detect_threshold`、`majority_ratio`。
