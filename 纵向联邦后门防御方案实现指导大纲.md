# 纵向联邦后门防御方案实现指导大纲

## 目标与范围
本大纲用于指导在 `shentt67/LFBA` 项目基础上实现一个新的纵向联邦后门防御方案。目标不是重写 LFBA，而是在现有攻击训练框架上增量加入可插拔的 anchor-based defense pipeline。

## 核心思路
1. 锚点预训练：先对每个被动方做单独的第一阶段训练，得到该被动方的本地表征模型与类别锚点。
2. 带锚点约束的联合训练：在原有 VFL 主任务损失上加入 anchor constraint loss。
3. 推理检测与纠正：每个客户端先基于自己的锚点空间给出本地预测，再用多数投票与全局预测做一致性检测，并在需要时执行标签纠正。

## 第一阶段的正确定义（补充说明）
这一部分是对原始大纲的关键补充，后续实现必须以本节为准。

### 1. 参与方职责
- 每个被动方只使用自己的特征输入，训练自己的本地模型（local backbone）。
- 分类头不属于被动方，分类头属于主动方。
- 主动方需要为每个被动方单独维护一个两层的 MLP 分类头，用于对该被动方的本地表示做类别监督。

### 2. 第一阶段训练方式
- 对于第 `i` 个被动方，输入仅为该被动方的本地特征 `x_i`。
- 被动方本地模型输出表示 `h_i`。
- 主动方上与之对应的两层 MLP 分类头接收 `h_i`，输出分类结果。
- 该阶段的监督目标是：让每个被动方单独学到具有类别判别性的表征空间，并由主动方分类头的类别权重形成该被动方的 anchor bank。
- 当前实现中，第一阶段不再只优化分类交叉熵，而是显式优化“分类 + 锚点对齐”的联合目标。
- 对第 `i` 个被动方，第一阶段损失定义为：
  - `L_stage1^(i) = L_ce^(i) + lambda_stage1_anchor * L_anchor_stage1^(i)`
- 其中：
  - `L_ce^(i)` 是该被动方本地表示经过主动方私有分类头后的交叉熵损失；
  - `L_anchor_stage1^(i) = (1 / B) * sum_j || z_i^(j) - w_{y_j}^{(i)} ||_2^2`
- 其中：
  - `B` 为 batch size；
  - `z_i^(j)` 表示第 `j` 个样本在第 `i` 个被动方上的 stage 1 anchor embedding；
  - `w_{y_j}^{(i)}` 表示第 `i` 个被动方中、标签 `y_j` 对应的类别锚点。
- 在当前实现中，`z_i` 的定义为：
  - `z_i = normalize(projector(h_i))`
- 而类别锚点来自对应私有分类头的分类层权重：
  - `w_c^(i) = normalize(classifier.weight[c])`
- 对图像数据集，尤其是 `CIFAR10` 这类按宽度切分后的多客户端局部图像视图，第一阶段不应默认假设每个被动方的局部输入都具备稳定的全类别判别语义。
- 已有实现与实验现象表明：在 `CIFAR10 + client_num > 2 + 窄图像切片` 的 stage 1 场景下，带正 angular margin 的 ArcFace 风格损失容易导致部分客户端塌缩，典型现象是：
  - `clean acc` 在第 2 个 epoch 左右迅速掉到约 `0.1`；
  - `anchor loss` 长时间卡在 `4.0`；
  - 预测退化为单类或极少数类别。
- 因此，对于这类图像分片场景，第一阶段更稳妥的默认做法是：
  - 可以保留归一化分类头或 anchor 机制；
  - 但 `anchor_margin` 应优先设为 `0.0`；
  - 不建议把正 margin 的 ArcFace 作为默认配置。

### 3. 攻击者在第一阶段的行为
- 攻击者在第一阶段也会进行后门注入。
- 但第一阶段的后门注入只影响两个对象：
  - 攻击者自己的本地模型。
  - 主动方为该攻击者分支维护的对应分类头。
- 其他诚实被动方在第一阶段只做干净训练，不参与攻击者的后门注入。
- 需要特别说明的是：
  - 第一阶段与第二阶段 / 测试阶段在语义上都是“攻击者进行后门注入”；
  - 只是实现方式不同。
- 当前代码中：
  - 第一阶段使用的是更适合单分支本地预训练的局部攻击构造；
  - 第二阶段 / 测试阶段使用的是完整 LFBA 联合训练与评估流程中的攻击构造。
- 因此，三者在“是否进行后门注入”这一点上是统一的，只是在不同阶段采用了不同的实现函数与数据构造方式。

### 4. 第一阶段需要输出的指标
- 每个被动方的干净分类准确率。
- 每个被动方的 anchor loss / 锚点相关损失。
- 攻击者分支在其“本地模型 + 主动方对应分类头”下的后门准确率（或 ASR）。
- 进入第一阶段时必须打印清晰提示，说明当前正在执行 stage 1。

### 5. 第一阶段需要保存的内容
- 每个被动方训练好的本地模型参数。
- 主动方为每个被动方维护的两层 MLP 分类头参数。
- 每个被动方的 anchor bank。
- 第二阶段要直接加载第一阶段已经训练好的被动方本地模型继续训练。

## 第二阶段的正确定义（补充说明）
这一部分用于明确“带锚点约束的联合训练”到底训练什么、固定什么、以及第一阶段产物在第二阶段中的角色。后续实现如果与本节冲突，以本节为准。

### 1. 第二阶段的核心目标
- 第二阶段回到标准的 VFL 联合训练流程。
- 所有被动方同时参与，每个被动方输入自己的局部特征，输出本地表示 `h_i`。
- 主动方全局模型接收所有被动方的本地表示，完成最终主任务分类。
- 在主任务损失之外，再额外加入 anchor constraint loss，用于约束每个被动方的表征不要偏离第一阶段得到的类别锚点空间。

### 2. 第二阶段的初始化来源
- 每个被动方的本地模型参数，必须从第一阶段保存的对应本地模型参数加载，而不是重新随机初始化。
- 主动方全局聚合模型可以按现有训练框架正常初始化，除非实验另有要求，一般不从第一阶段继承。
- 第一阶段的两层 MLP 分类头在第二阶段不再承担分类训练职责，但需要被加载出来，用于恢复：
  - 每个被动方对应的 projector / embedding 映射；
  - 每个被动方对应的类别 anchor bank。
- 因此，第一阶段分类头在第二阶段的角色是“冻结的锚点空间定义器”，而不是“继续训练的分类器”。

### 3. 第二阶段哪些参数应当训练
- 需要继续训练的参数：
  - 主动方全局模型参数；
  - 各个被动方本地模型参数。
- 默认不继续训练的参数：
  - 第一阶段保存的两层 MLP 分类头参数；
  - 由分类头权重导出的 anchor bank。
- 也就是说，第二阶段中 anchor 相关模块默认作为固定参照系存在，只为损失计算和检测服务，不参与优化更新。

### 4. 第二阶段的前向计算
- 对于一个 batch 中的样本 `x`，先按 VFL 方式切分为各个被动方特征 `x_1, x_2, ..., x_K`。
- 第 `i` 个被动方本地模型输出其本地表示 `h_i`。
- 主动方全局模型聚合所有 `h_i`，输出主任务分类结果。
- 同时，对每个 `h_i` 再经过第一阶段对应分类头中的 projector，映射到 anchor 空间，得到 `z_i`。
- 再根据当前样本标签 `y`，从第 `i` 个被动方的 anchor bank 中取出对应类别锚点 `w_y^(i)`。
- 最后计算 `z_i` 与 `w_y^(i)` 的距离，并在所有客户端、所有样本上做平均，得到 batch 的 anchor constraint loss。

### 5. 第二阶段的损失函数
- 第二阶段总损失定义为：
  - `L = L_ce + lambda_anchor * L_anchor`
- 其中：
  - `L_ce` 是主动方全局模型输出上的主任务交叉熵损失；
  - `L_anchor` 是多客户端 anchor constraint loss。
- 推荐使用如下形式：
  - `L_anchor = (1/B) * sum_j (1/K) * sum_i || z_i^(j) - w_{y_j}^{(i)} ||_2^2`
- 其中：
  - `B` 为 batch size；
  - `K` 为被动方数量；
  - `z_i^(j)` 为第 `j` 个样本在第 `i` 个被动方上的投影表示；
  - `w_{y_j}^{(i)}` 为第 `i` 个被动方中、第 `j` 个样本标签对应的 anchor。

### 6. 第二阶段中攻击者的行为
- 第二阶段仍然允许保留原 LFBA / LRA / RSA 等攻击训练流程。
- 与无防御版本相比，差别不在于“去掉攻击”，而在于攻击和正常主任务训练都要同时接受 anchor loss 的约束。
- 也就是说，攻击者分支在试图诱导后门行为的同时，会被拉向第一阶段形成的类别锚点空间。
- 第一阶段中“只有攻击者自己的分支被污染”的约束只针对第一阶段预训练；进入第二阶段后，训练流程重新回到原有联合训练和攻击注入逻辑。

### 7. 第二阶段的日志与可观测指标
- 每个 epoch 至少需要打印：
  - 主任务训练损失；
  - anchor loss；
  - 主任务训练准确率；
  - 测试集 clean acc；
  - 测试集 ASR；
  - 测试集 target-class acc；
  - 第三阶段最终 clean acc；
  - 第三阶段最终 ASR；
  - recall；
  - precision；
  - f1；
  - correction rate；
  - false positive rate。
- 如果启用了 early stop 或最佳模型保存，需要明确基于哪个 trade-off 指标进行比较。
- 当前代码中，最佳 checkpoint 仍默认基于 `test_acc` 选择，而不是基于检测 `f1` 选择。
- 推荐至少将 `test_acc`、`test_asr`、`stage3_final_asr`、`recall`、`precision`、`f1`、`correction_rate`、`false_positive_rate` 与 `anchor_loss` 一并写入 checkpoint 或日志。

### 8. 第二阶段需要保存的内容
- 主动方全局模型参数。
- 所有被动方本地模型参数。
- 优化器状态。
- 当前最佳性能指标。
- anchor defense 的必要状态：
  - 冻结分类头参数或其等价表示；
  - anchor bank；
  - 检测器所需统计量（如果实现中有持久化需要）。
- 注意：第二阶段 checkpoint 的目标是支持“恢复训练”和“测试评估”，而不是替代第一阶段原始 artifact。

## 第三阶段的定义
- 第三阶段检测默认依赖第二阶段训练结束后的模型参数和 anchor defense 状态。
- 推理时，先得到全局模型预测类别 `pred_label`。
- 对于第 `i` 个客户端，先将其本地表示通过冻结的 projector 映射到 anchor 空间，得到 `z_i`。
- 然后计算该客户端到所有类别锚点的距离：
  - `d_i(c) = || z_i - w_c^(i) ||_2^2`
- 该客户端的本地锚点预测定义为：
  - `v_i = argmin_c d_i(c)`
- 对所有客户端的本地锚点预测做多数投票，得到：
  - `majority_label = MajorityVote(v_1, v_2, ..., v_K)`
- 若 `majority_label` 与全局预测 `pred_label` 一致，且该多数票达到严格多数：
  - `required_votes = floor(K / 2) + 1`
- 则认为该样本预测有效，不触发第三阶段纠正。
- 否则，将该样本判为可疑后门样本，并将最终输出标签修正为 `majority_label`。
- 当前第三阶段的核心不再是“基于客户端 anchor loss 阈值判异常”，而是“基于各客户端锚点预测与全局预测的一致性判可疑”。
- 该机制默认适用于攻击者数量少于诚实客户端数量的场景；如果攻击者已占多数，则第三阶段纠正能力会明显下降。

### 第三阶段检测与纠正指标
- 当前项目中，第三阶段既承担检测任务，也承担纠正任务，因此评估时应同时报告检测指标和纠正指标。
- 记：
  - `S_success` 为原始预测已经被后门攻击成功的样本集合，即在 poisoned test 中满足 `y != target_label` 且原始预测 `pred = target_label` 的样本；
  - `S_detect` 为被第三阶段判为可疑样本的集合；
  - `S_correct` 为经过第三阶段纠正后最终恢复为真实标签的样本集合。
- 第三阶段检测 `Recall` 定义为：
  - `Recall = | S_success ∩ S_detect | / | S_success |`
- 该定义下，`Recall` 只以“原始已经攻击成功的危险样本”为正类，而不是以全部 poisoned 样本为正类。
- 第三阶段检测 `Precision` 定义为：
  - `Precision = | S_success ∩ S_detect | / (| S_success ∩ S_detect | + FP_clean + FP_poison_failed)`
- 其中：
  - `FP_clean` 表示 clean test 中被误判为可疑的样本数；
  - `FP_poison_failed` 表示 poisoned test 中原始并未攻击成功、但被误判为可疑的样本数。
- 第三阶段检测 `F1` 定义为：
  - `F1 = 2 * Precision * Recall / (Precision + Recall)`
- 第三阶段纠正率 `Correction Rate` 定义为：
  - `Correction Rate = | S_success ∩ S_correct | / | S_success |`
- 该指标表示：原始已经攻击成功的样本中，有多少被第三阶段真正纠正回了真实标签。
- 额外保留 clean 误报率 `False Positive Rate` 作为补充指标：
  - `False Positive Rate = FP_clean / | Clean Test Set |`

## 建议新增目录
- `defense/anchor_losses.py`
- `defense/anchor_utils.py`
- `defense/anchor_trainer.py`
- `defense/anchor_defense.py`
- `defense/detector.py`

## 关键实现要求
### 1. 数据结构
- `anchor_bank = {client_id: tensor[num_classes, feat_dim]}`
- `feature_dict = {client_id: h_i}`

### 2. 锚点预训练
- 被动方：`local backbone`
- 主动方：为每个被动方单独维护一个两层 `MLP classifier`
- 分类头权重作为类别锚点来源
- 第一阶段联合损失：
  - `L_stage1 = L_ce + lambda_stage1_anchor * L_anchor`
- 其中：
  - `L_anchor = (1/B) * sum_j || z_i^(j) - w_{y_j}^{(i)} ||_2^2`
- `z_i` 由 `normalize(projector(h_i))` 得到，`w_y` 来自私有分类头分类层的归一化权重
- 可以使用 ArcFace 风格损失，但图像分片场景需要谨慎
- 对 `CIFAR10` 等图像数据集，如果输入是按宽度切开的局部窄视图，推荐先使用 `anchor_margin = 0.0`
- 如果后续要重新尝试正 margin，必须把它当作待调超参，而不是默认安全配置
- 当前优先保证二分类和简单数据集版本可运行

### 3. 联合训练
- `L = L_ce + lambda_anchor * L_anchor`
- `L_anchor = (1/B) * sum_j sum_i || z_i^(j) - w_{y_j}^{(i)} ||_2^2`
- 第二阶段直接从第一阶段保存的被动方本地模型开始继续训练
- 第一阶段分类头在第二阶段默认冻结，只保留其 projector / anchor 定义功能
- 第二阶段真正参与优化的是全局模型与各被动方本地模型
- 第二阶段的 anchor loss 更准确地说应当约束投影后的表示 `z_i`，而不是必须直接约束原始 `h_i`
- 第二阶段训练结束后，应当能直接进入第三阶段检测，不需要重新做第一阶段

### 4. 推理检测
- 每个客户端先根据到各类别锚点的距离给出本地预测 `v_i = argmin_c d_i(c)`
- 对所有客户端本地预测做多数投票，得到 `majority_label`
- 若 `majority_label` 与全局预测一致且达到严格多数，则认为该样本预测有效
- 否则，将该样本识别为可疑后门样本，并把最终标签修正为 `majority_label`
- 第三阶段评估时应至少输出：
  - `recall`
  - `precision`
  - `f1`
  - `correction rate`
  - `false positive rate`

## main.py 修改
新增参数：
- `--mode`
- `--defense`
- `--lambda_anchor`
- `--lambda_stage1_anchor`
- `--anchor_pretrain_epochs`
- `--anchor_scale`
- `--anchor_margin`
- `--anchor_bank_path`
- `--anchor_stage1_dir`
- `--detect_threshold`
- `--detect_threshold_cap`
- `--majority_ratio`

## 建议开发顺序
1. 先实现第一阶段：被动方本地模型 + 主动方两层 MLP 分类头 + 攻击者分支后门注入。
2. 再接入带锚点约束的 VFL 联合训练。
3. 最后实现检测与评估。

## 容易踩坑
- 不同客户端特征维度不同，锚点不能直接拼接。
- 第一阶段的分类头在主动方，不在被动方。
- 攻击者第一阶段的注入只应该污染攻击者自己的分支，不应该污染其他诚实被动方。
- 第二阶段不要误把第一阶段分类头当作“继续训练的分类器”；默认它们应当被冻结，只作为 projector 和 anchor 来源。
- 第二阶段不要重新随机初始化被动方本地模型，否则第一阶段表征预训练就失去意义。
- 第二阶段的 checkpoint 与第一阶段 artifact 不是一回事：前者用于联合训练恢复，后者用于提供锚点空间和初始本地模型。
- 推理阶段默认按各客户端最近锚点类别做本地预测，再与全局预测做多数一致性比较。
- 优先在 PHISHING 这类较小数据集上跑通。
- 当前本地复现实验中，UCIHAR 数据集上的 LFBA 攻击成功率明显偏低，与原论文报告现象不符。
- 因此，后续防御方案实现、对比实验与结果展示默认不再使用 UCIHAR 作为主要实验数据集，避免结论被异常复现结果干扰。
- 对 `CIFAR10 + client_num > 2` 这类图像切片场景，不要默认使用正 `anchor_margin`。
- 如果 stage 1 日志出现 `clean acc` 从首轮正常值迅速跌到约 `0.1`，同时 `anchor loss` 卡在 `4.0`，优先怀疑是 ArcFace margin 导致的塌缩，而不是先怀疑评估代码。
- 在 `PHISHING` 数据集的本地复现实验中，曾经出现过“client 1 的 stage 1 anchor loss 长时间维持在 `2.7+`，明显高于其他客户端”的现象。
- 该问题的主要原因不是单一超参，而是以下因素叠加：
  - `PHISHING` 特征按连续维度切分后，client 1 拿到的那段局部特征判别性较弱；
  - 原始本地模型结构过浅，客户端输入大约 `37` 维时，使用的是过于激进的 `input -> 8 -> 4` 压缩；
  - stage 1 私有 projector 也较浅，只能在已经压缩到低维的表示上做有限整理；
  - 因此会出现“clean acc 还能维持，但 anchor loss 难以下降”的现象。
- 最终采取的解决办法是两步：
  - 将第一阶段损失从“仅优化交叉熵”改为“交叉熵 + 显式 anchor loss”，即 `L_stage1 = L_ce + lambda_stage1_anchor * L_anchor`；
  - 加深 `PHISHING` 被动方本地模型，将其从 `input -> 8 -> 4` 调整为更宽更深的 `input -> 32 -> 16 -> 8`，为 anchor 空间学习保留更充足的表示容量。
- 如果后续仍出现某个客户端 `clean acc` 正常但 `anchor loss` 很高的情况，应优先从“局部特征切分是否过弱”和“local model / projector 表达能力是否不足”两个方向排查，而不是先假设检测逻辑有误。

## 可直接给代码生成 AI 的任务说明
请基于 GitHub 项目 `shentt67/LFBA` 的现有代码结构，实现一个新的纵向联邦后门防御方案，尽量复用 `main.py`、`models`、`dataset` 和 `trainer` 结构，不要推翻重写。实现一个 Anchor-based defense，包含三个阶段：
1. 第一阶段锚点预训练：被动方只用自己的特征输入，主动方为每个被动方维护一个两层 MLP 分类头；攻击者在这一阶段也进行后门注入，但只影响自己的本地模型和主动方对应分类头。
2. 第二阶段带锚点约束的 VFL 联合训练。
3. 第三阶段推理检测。

请输出 clean acc、ASR、anchor loss、recall、precision、f1、correction rate 与 false positive rate，并保证第一阶段单独可观测、可保存、可复用。


## 数据集来源
phishing： https://github.com/GregaVrbancic/Phishing-Dataset?tab=readme-ov-file
