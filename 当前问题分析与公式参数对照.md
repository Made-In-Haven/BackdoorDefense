# 当前问题分析与公式参数对照

本文档用于两件事：

1. 总结当前 `PHISHING` 数据集在第三阶段纠正率异常偏低的问题。
2. 将代码中的关键参数、张量和计算过程，与 [纵向联邦后门防御方案实现指导大纲_含修改标记.md](纵向联邦后门防御方案实现指导大纲_含修改标记.md) 中的公式符号逐项对应起来，便于后续排查与论文/方案同步。

---

## 1. 当前问题总结

### 1.1 现象

在 `results/AVGuard/PHISHING/PHISHING_anchor_defense_stage2/experiment.log` 中，第三阶段的检测指标并不低，但纠正率持续接近 0。例如：

- `Stage 3 Detection Summary: recall: 0.8074, precision: 0.8082, f1: 0.8078, false positive rate: 0.0526, correction rate: 0.0000`
- `Stage 3 Debug (attack-success): suspicious=4033/4995, correction_applied=4033, prediction_changed=0, corrected_to_true=0`

这说明：

- 第三阶段已经把大量攻击成功样本判为了 `suspicious`。
- 也已经进入了 `correction_applied` 分支。
- 但最终预测并没有真正被改回真实标签，导致 `corrected_to_true=0`。

### 1.2 批级调试得到的直接证据

在 `stage3_test.json` 开启批级调试后，日志中出现了如下异常：

- `dynamic_weights` 中出现 `NaN`
- `dynamic_vote_scores` 中出现 `NaN`

典型形式如下：

- `dynamic_weights: [NaN, NaN, 0.0]`
- `dynamic_weights: [NaN, NaN, NaN]`
- `dynamic_vote_scores: [0.0, NaN]`
- `dynamic_vote_scores: [NaN, NaN]`

这说明第三阶段联合加权投票的数值已经不稳定，后续得到的 `weighted_labels` 不再可靠。

### 1.3 当前最可能的根因

当前第三阶段在 `defense/detector.py` 中直接计算：

```python
weighted_scores = static_reliability_vector.unsqueeze(0) * torch.exp(self.gamma * confidence_matrix)
```

而当前局部置信度定义为：

$$
q_i=\frac{d_i^{(2)}-d_i^{(1)}}{d_i^{(1)}+\varepsilon}
$$

当某些样本上 `d_i^(1)` 很小、`q_i` 很大时，`exp(gamma * q_i)` 很容易数值溢出，进而出现：

- `inf`
- `inf / inf`
- `NaN`

然后传导到：

- `dynamic_weight_matrix`
- `dynamic_vote_scores`
- `weighted_labels`
- `final_predictions`

因此，当前 `PHISHING` 上“纠正率低”的首要原因，更像是第三阶段数值稳定性 bug，而不是单纯的参数没有调好。

### 1.4 当前影响

这个问题会直接带来两个后果：

1. `correction_applied` 和真正“纠正成功”脱钩。
2. 第三阶段的投票标签可能已经被 `NaN` 污染，从而出现“检测到了但没改对”的现象。

### 1.5 当前建议

后续优先修复方向应为：

1. 将 `exp(gamma * q_i)` 改为数值稳定版本，例如先减去行内最大值再做指数变换。
2. 在权重归一化前后对 `NaN/inf` 做显式清洗。
3. 保留当前批级调试输出，用于确认 `attack-success-suspicious` 样本的 `weighted_labels` 是否恢复正常。

---

## 2. 文档公式与代码实现对照

下面按三个阶段，将方案文档中的公式符号和代码中的具体对象对应起来。

---

## 3. 第一阶段：局部锚点预训练

### 3.1 局部表示与锚点空间

文档公式：

$$
h_i = f_i(x_i)
$$

代码对应：

- `f_i`：第 `i` 个被动方本地模型，对应 `model_list[i + 1]`
- `h_i`：本地表示，对应 `features = local_model(x_split_list[client_id])`
- 主要位置：`defense/anchor_trainer.py`

文档公式：

$$
z_i = \operatorname{normalize}(g_i(h_i))
$$

代码对应：

- `g_i`：第一阶段私有头中的 `projector`
- `z_i`：归一化后的锚点空间表示
- 主要实现：
  - `defense/anchor_losses.py`
  - `ArcFaceClassifier.projector`
  - `ArcFaceClassifier.embed()`

### 3.2 类别锚点

文档公式：

$$
w_c^{(i)} = \operatorname{normalize}(W_c^{(i)})
$$

代码对应：

- `W_c^(i)`：`ArcFaceClassifier.classifier.weight`
- `w_c^(i)`：归一化后的分类器权重
- 第一阶段单客户端训练时通过

```python
anchors = F.normalize(head.classifier.weight, dim=1).index_select(0, labels)
```

得到

- 保存到阶段工件后，对应 `AnchorDefense.anchor_bank[client_id]`

### 3.3 第一阶段损失

文档公式：

$$
L_{\text{stage1}}^{(i)} = L_{\text{ce}}^{(i)} + \lambda_{\text{stage1-anchor}} L_{\text{anchor-stage1}}^{(i)}
$$

代码对应：

- `L_ce^(i)`：

```python
ce_loss = F.cross_entropy(logits, y)
```

- `L_anchor-stage1^(i)`：

```python
anchor_loss = compute_single_client_anchor_loss(head, features, y)
```

- `lambda_stage1-anchor`：
  - 代码参数名：`args.lambda_stage1_anchor`
  - 配置字段名：`lambda_stage1_anchor`
  - 入口：`main.py`

文档公式：

$$
L_{\text{anchor-stage1}}^{(i)} = \frac{1}{B}\sum_{j=1}^{B}\left\| z_i^{(j)} - w_{y_j}^{(i)} \right\|_2^2
$$

代码对应：

```python
return ((embeddings - anchors) ** 2).sum(dim=1).mean()
```

其中：

- `embeddings` 对应 \(z_i^{(j)}\)
- `anchors` 对应 \(w_{y_j}^{(i)}\)

### 3.4 第一阶段输出与静态可靠度来源

文档符号：

$$
\text{Acc}_i^{(1)}
$$

代码对应：

- 每个客户端第一阶段验证集 `clean_acc`
- 记录位置：
  - `defense/anchor_trainer.py`
  - `stage1_metrics.json`
- 后续作用：
  - 用于构造第三阶段静态可靠度 \(r_i\)

工程参数补充：

- `anchor_pretrain_epochs`：第一阶段总轮数
- `anchor_pretrain_early_stop`：第一阶段早停耐心值
- `anchor_pretrain_min_delta`：将 `clean_acc` 视为有效提升所需的最小增量

这三个参数属于工程训练控制项，不在文档公式中直接记号化，但直接影响 `Acc_i^(1)` 的最终取值。

### 3.5 ArcFace 相关实现参数

这两个参数没有在文档公式中单独命名，但对应第一阶段和第二阶段锚点头的内部超参数：

- `anchor_scale`：对应 ArcFace 中 logits 缩放系数 `scale`
- `anchor_margin`：对应 ArcFace 中角度间隔 `margin`

主要实现位于 `defense/anchor_losses.py` 的 `ArcFaceClassifier`。

---

## 4. 第二阶段：带锚点约束与 EMA 校准的联合训练

### 4.1 全局预测

文档公式：

$$
\hat{y} = F(h_1, h_2, \dots, h_K)
$$

代码对应：

- `F`：全局模型，对应 `model_list[0]`
- `h_1, ..., h_K`：各被动方本地输出 `local_output_list`
- `\hat y`：`global_output.argmax(dim=1)`

主要位置：

- `utils/trainer.py`
- `defense/anchor_defense.py`

### 4.2 第二阶段锚点约束损失

文档公式：

$$
L_{\text{anchor}} = \frac{1}{B}\sum_{j=1}^{B}\frac{1}{K}\sum_{i=1}^{K}
\left\| z_i^{(j)} - w_{y_j}^{(i)} \right\|_2^2
$$

代码对应：

- 核心实现函数：`compute_anchor_constraint_loss(...)`
- 文件：`defense/anchor_losses.py`

代码中先逐客户端计算：

```python
client_loss = ((embeddings - anchors) ** 2).sum(dim=1).mean()
```

再对所有客户端平均：

```python
total_loss = sum(loss_list) / len(loss_list)
```

文档公式：

$$
L = L_{\text{ce}} + \lambda_{\text{anchor}} L_{\text{anchor}}
$$

代码对应：

- `L_ce`：主任务交叉熵
- `L_anchor`：多客户端锚点约束损失
- `lambda_anchor`：
  - 代码参数名：`args.lambda_anchor`
  - 配置字段名：`lambda_anchor`
  - 训练位置：`utils/trainer.py`

### 4.3 EMA 锚点校准

文档公式：

$$
\mu_c^{(i)} = \frac{1}{N_c}\sum_{j:y_j=c} z_i^{(j)}
$$

代码对应：

- 每轮训练中先累计每一类的 embedding sum 和 class count
- 然后在 `apply_epoch_anchor_ema(...)` 中计算类均值
- 对应代码变量：
  - `anchor_statistics["embedding_sums"][client_id]`
  - `anchor_statistics["class_counts"][client_id]`
  - `class_means`

文档公式：

$$
w_c^{(i)} \leftarrow \operatorname{normalize}
\left(
m\, w_c^{(i)} + (1-m)\, \mu_c^{(i)}
\right)
$$

代码对应：

```python
blended_anchor = momentum * anchor[updated_mask] + (1.0 - momentum) * class_means
anchor[updated_mask] = F.normalize(blended_anchor, dim=1)
```

其中：

- \(m\) 对应代码参数 `anchor_ema_momentum`
- 配置字段名：`anchor_ema_momentum`
- EMA 更新频率对应 `anchor_ema_update_freq`

---

## 5. 第三阶段：基于有效支持人数与联合加权投票的检测与纠正

### 5.1 全局预测与局部锚点预测

文档符号：

$$
\hat y_{\text{global}}
$$

代码对应：

- `predicted_labels = global_output.argmax(dim=1)`
- 文件：`defense/anchor_defense.py`

文档公式：

$$
d_i(c)=\left\|z_i-w_c^{(i)}\right\|_2^2
$$

代码对应：

```python
distance_dict[client_id] = ((embeddings.unsqueeze(1) - anchors.unsqueeze(0)) ** 2).sum(dim=2)
```

文件：`defense/anchor_defense.py`

文档公式：

$$
v_i=\arg\min_c d_i(c)
$$

代码对应：

```python
client_distances.argmin(dim=1)
```

对应变量：

- `client_prediction_dict[client_id]`

### 5.2 局部置信度

文档公式：

$$
d_i^{(1)}=\min_c d_i(c)
$$

$$
d_i^{(2)}=\min_{c\neq v_i} d_i(c)
$$

$$
q_i=\frac{d_i^{(2)}-d_i^{(1)}}{d_i^{(1)}+\varepsilon}
$$

代码对应：

- 计算位置：`defense/detector.py::_compute_client_confidences`
- 代码变量：
  - `nearest_distances` 对应 \(d_i^{(1)}\)
  - `second_nearest_distances` 对应 \(d_i^{(2)}\)
  - `confidence_matrix[:, column_id]` 对应 \(q_i\)
- `epsilon`：`self.epsilon = 1e-8`

### 5.3 有效支持人数

文档公式：

$$
\mathbb I_i^{\text{valid}} = \mathbf 1\Big(v_i=\hat y_{\text{global}}\ \text{and}\ q_i\ge \theta_{\text{supp}}\Big)
$$

代码对应：

```python
valid_support_mask = stacked_predictions.eq(global_predictions.unsqueeze(1)) & confidence_matrix.ge(
    self.theta_supp
)
```

其中：

- `stacked_predictions.eq(global_predictions.unsqueeze(1))` 对应 \(v_i=\hat y_{\text{global}}\)
- `confidence_matrix.ge(self.theta_supp)` 对应 \(q_i \ge \theta_{\text{supp}}\)
- `theta_supp`：
  - 代码参数名：`args.theta_supp`
  - 配置字段名：`theta_supp`

文档公式：

$$
N_{\text{valid-agree}} = \sum_{i=1}^{K} \mathbb I_i^{\text{valid}}
$$

代码对应：

- `valid_support_counts = valid_support_mask.sum(dim=1)`

文档公式：

$$
N_{\text{valid-agree}} \ge \left\lfloor \frac{K}{2} \right\rfloor + 1
$$

代码对应：

- `strict_majority_votes = (self.client_num // 2) + 1`
- `suspicious_mask = valid_support_counts.lt(strict_majority_votes)`

### 5.4 剩余客户端集合

文档公式：

$$
\mathcal{S}_{\text{remain}}=\{i \mid v_i \neq \hat{y}_{\text{global}} \}
$$

代码对应：

- `remaining_mask = stacked_predictions.ne(global_predictions.unsqueeze(1))`
- `remaining_client_mask_dict`
- `remaining_counts`

### 5.5 静态可靠度

文档公式：

$$
r_i = \frac{\text{Acc}_i^{(1)}}{\sum_{k=1}^{K}\text{Acc}_k^{(1)}}
$$

代码对应：

- 第一阶段指标输入：`stage1_metrics[client_id]["clean_acc"]`
- 构造位置：`AnchorDefense._build_client_reliability(...)`
- 运行时变量：
  - `self.client_reliability`
  - `static_reliability_vector`

文档中的 \(r_i\) 在代码里没有单独保留为标量变量名，而是以以下形式出现：

- `client_reliability`
- `client_reliability_vector`

### 5.6 联合权重

文档公式：

$$
\tilde{\alpha}_i = \frac{r_i\exp(\gamma q_i)}{\sum_{k\in\mathcal S_{\text{remain}}} r_k\exp(\gamma q_k)}, \quad i\in\mathcal S_{\text{remain}}
$$

代码对应：

```python
log_reliability = torch.log(static_reliability_vector.clamp_min(self.reliability_log_epsilon)).unsqueeze(0)
joint_scores = log_reliability + self.gamma * confidence_matrix[valid_weight_rows]
masked_joint_scores = torch.where(valid_remaining_mask, joint_scores, torch.full_like(joint_scores, -1e4))
shifted_scores = masked_joint_scores - masked_joint_scores.max(dim=1, keepdim=True).values
exp_scores = torch.exp(shifted_scores) * valid_remaining_mask.float()
dynamic_weight_matrix[valid_weight_rows] = exp_scores / weight_denominator
```

其中：

- `self.gamma` 对应文档中的 \(\gamma\)
- `confidence_matrix` 对应 \(q_i\)
- `static_reliability_vector` 对应 \(r_i\)
- `dynamic_weight_matrix` 对应 \(\tilde{\alpha}_i\)

说明：

- 当前实现已经将原始联合加权公式改写为“`log(r_i)` 打分 + 最大值平移 + stable softmax”的等价稳定形式，用来避免指数溢出带来的 `NaN/inf`。

### 5.7 联合加权投票分数

文档公式：

$$
\text{Score}(c)=\sum_{i\in\mathcal S_{\text{remain}}}\tilde{\alpha}_i \cdot \mathbf 1(v_i=c)
$$

代码对应：

```python
dynamic_vote_scores.scatter_add_(1, stacked_predictions, dynamic_weight_matrix)
```

其中：

- `dynamic_vote_scores[:, c]` 对应 \(\text{Score}(c)\)

### 5.8 最终纠正标签

文档公式：

$$
\hat y_{\text{final}} = \arg\max_c \text{Score}(c)
$$

代码对应：

```python
weighted_labels = dynamic_vote_scores.argmax(dim=1)
corrected_predictions[correction_applied_mask] = detector_output["weighted_labels"][correction_applied_mask]
```

变量对应：

- `weighted_labels`：纠正投票得到的候选标签
- `final_predictions`：最终输出标签
- `correction_applied_mask`：实际执行替换的位置

若启用保守纠正机制，则代码还会额外计算：

- `top_vote_scores`：对应 \(\text{Score}^{(1)}\)
- `second_vote_scores`：对应 \(\text{Score}^{(2)}\)
- `correction_margin`：对应 \(\Delta_{\text{corr}}=\text{Score}^{(1)}-\text{Score}^{(2)}\)
- `tau_corr`：对应 \(\tau_{\text{corr}}\)

此时仅当：

- 样本已被判定为 `suspicious`
- 剩余客户端存在有效纠正票
- 不存在 weighted tie
- `correction_margin >= tau_corr`

时，`correction_applied_mask` 才为真。

---

## 6. 代码参数与公式符号总表

| 代码参数/字段 | 文档公式符号 | 含义 | 主要使用位置 |
| --- | --- | --- | --- |
| `lambda_stage1_anchor` | \(\lambda_{\text{stage1-anchor}}\) | 第一阶段锚点损失权重 | `main.py`, `defense/anchor_trainer.py` |
| `lambda_anchor` | \(\lambda_{\text{anchor}}\) | 第二阶段锚点约束损失权重 | `main.py`, `utils/trainer.py` |
| `anchor_ema_momentum` | \(m\) | EMA 锚点更新动量 | `main.py`, `defense/anchor_defense.py` |
| `gamma` | \(\gamma\) | 第三阶段联合加权温度系数 | `main.py`, `defense/detector.py` |
| `theta_supp` | \(\theta_{\text{supp}}\) | 第三阶段有效支持阈值 | `main.py`, `defense/detector.py` |
| `tau_corr` | \(\tau_{\text{corr}}\) | 保守纠正所需的最小投票 margin | `main.py`, `defense/detector.py` |
| `clean_acc` in `stage1_metrics.json` | \(\text{Acc}_i^{(1)}\) | 第一阶段客户端 clean accuracy | `defense/anchor_trainer.py`, `defense/anchor_defense.py` |
| `client_reliability` | \(r_i\) | 由第一阶段 `clean_acc` 归一化得到的静态可靠度 | `defense/anchor_defense.py`, `defense/detector.py` |
| `confidence_matrix` | \(q_i\) | 样本级动态置信度 | `defense/detector.py` |
| `dynamic_weight_matrix` | \(\tilde{\alpha}_i\) | 剩余客户端联合投票权重 | `defense/detector.py` |
| `dynamic_vote_scores` | \(\text{Score}(c)\) | 各候选类别的联合投票分数 | `defense/detector.py` |
| `weighted_labels` | \(\arg\max_c \text{Score}(c)\) | 投票纠正标签 | `defense/detector.py`, `defense/anchor_defense.py` |
| `final_predictions` | \(\hat y_{\text{final}}\) | 第三阶段最终输出标签 | `defense/anchor_defense.py`, `utils/trainer.py` |

---

## 7. 无直接公式符号、但与实现强相关的工程参数

以下参数不直接出现在方案文档公式中，但对实验行为有明显影响：

- `anchor_pretrain_epochs`
  - 控制第一阶段每个客户端最多训练多少轮。

- `anchor_pretrain_early_stop`
  - 控制第一阶段早停耐心值。

- `anchor_pretrain_min_delta`
  - 控制第一阶段 `clean_acc` 至少提升多少才算“有进步”。
- 

- `anchor_scale`
  - ArcFace 缩放系数。

- `anchor_margin`
  - ArcFace 角度间隔。

- `anchor_ema_update_freq`
  - 第二阶段 EMA 锚点更新频率。

- `enable_conservative_correction`
  - 第三阶段是否启用保守纠正 margin 门槛。

- `stage3_debug_batches`
  - 调试参数，控制第三阶段额外打印多少个批次。

- `stage3_debug_max_samples`
  - 调试参数，控制每个批次最多打印多少个样本。

---

## 8. 当前实现与方案公式的一致性说明

当前工程主线已经与方案文档中的三阶段结构保持一致：

1. 第一阶段输出局部模型、私有锚点头、锚点库和 `clean_acc`。
2. 第二阶段使用锚点约束损失并做 EMA 锚点校准。
3. 第三阶段基于“有效支持人数 + 静态可靠度 + 动态置信度”进行检测与纠正。

但当前 `PHISHING` 实验暴露出一个实现层面的偏差：

- 文档中的联合权重 \(\tilde{\alpha}_i\) 默认是有限、可归一化的。
- 当前代码中的 `torch.exp(self.gamma * confidence_matrix)` 在部分样本上会溢出，导致 `dynamic_weight_matrix` 和 `dynamic_vote_scores` 出现 `NaN`。

因此，方案公式本身已经同步到代码，但第三阶段联合加权的数值稳定性还需要进一步修正，才能让实现行为真正与方案预期保持一致。









可以。下面给你一版**专门为了降低 FPR 的第三阶段改进公式**。思路是：

* **检测阶段**仍然保留你现在的“有效支持人数”机制，不轻易推翻
* **纠正阶段**新增一道“纠正门控”，避免 suspicious 之后立刻强制改标签
* **联合权重**继续保留，但建议在公式层面写成更平滑、可控的形式

你当前第三阶段已经是“有效支持人数 + 排除同向客户端 + 联合加权投票纠正”的结构，所以这版是在此基础上继续加一层**保守纠正条件**，目的是减少 clean 样本误改，从而压低 FPR。 

---

# 一、第三阶段改进目标

当前问题不是第三阶段不会纠正，而是：

* 很多样本被判为 suspicious
* 一旦 suspicious，几乎就直接进入纠正并改标签
* 从而导致 clean 样本误改偏多，FPR 偏高 

因此，改进目标是：

[
\text{Suspicious} ;\not\Rightarrow; \text{Must Correct}
]

也就是：

> 样本可以先被检测为可疑，但只有当纠正证据足够强时，才真正替换全局预测。

---

# 二、检测阶段保持不变

## 3.2 基于有效支持人数的可疑样本判定

先保留你现在的检测规则：

[
\mathbb I_i^{\text{valid}} = \mathbf 1\Big(v_i=\hat y_{\text{global}}\ \text{and}\ q_i\ge \theta_{\text{supp}}\Big)
]

[
N_{\text{valid-agree}} = \sum_{i=1}^{K} \mathbb I_i^{\text{valid}}
]

若

[
N_{\text{valid-agree}} \ge \left\lfloor \frac{K}{2} \right\rfloor + 1
]

则直接输出：

[
\hat y_{\text{final}}=\hat y_{\text{global}}
]

否则，样本进入可疑样本集合，并进入后续纠正阶段。这个检测思路本身和你当前文档保持一致。

---

# 三、纠正阶段的核心改进：加入“纠正门控”

---

## 3.3 可疑样本的客户端筛选

这一步也保持不变，继续排除与全局预测同向的客户端：

[
\mathcal{S}*{\text{remain}}={i \mid v_i \neq \hat{y}*{\text{global}} }
]

这样做仍然是合理的，因为后门样本中与全局预测同向的客户端更可能已经受到攻击影响。

---

## 3.4 联合权重

对剩余客户端继续定义联合权重。建议使用你前面已经同步的数值稳定写法。先定义联合得分：

[
s_i=\log(r_i+\delta_r)+\gamma q_i
]

其中：

* (r_i)：客户端级静态可靠度
* (q_i)：样本级动态置信度
* (\gamma)：温度系数
* (\delta_r)：防止对零取对数的极小常数

再做稳定化平移：

[
s_i' = s_i-\max_{k\in\mathcal S_{\text{remain}}} s_k
]

最终联合权重定义为：

[
\tilde{\alpha}_i
================

\frac{\exp(s_i')}
{\sum_{k\in\mathcal S_{\text{remain}}}\exp(s_k')}
,\quad i\in\mathcal S_{\text{remain}}
]

这和你之前的联合加权思想等价，但实现上更稳定。

---

## 3.5 联合加权投票分数

对每个候选类别 (c)，计算纠正侧加权投票得分：

[
\text{Score}(c)=\sum_{i\in\mathcal S_{\text{remain}}}\tilde{\alpha}_i \cdot \mathbf 1(v_i=c)
]

记第一高分对应类别为：

[
c^{*}=\arg\max_c \text{Score}(c)
]

并记：

[
\text{Score}^{(1)}=\max_c \text{Score}(c)
]

[
\text{Score}^{(2)}=\max_{c\neq c^*}\text{Score}(c)
]

---

# 四、新增公式 1：纠正 margin 门槛

定义纠正侧的投票置信 margin 为：

[
\Delta_{\text{corr}}=\text{Score}^{(1)}-\text{Score}^{(2)}
]

它表示：

> 当前纠正候选标签 (c^*) 相比第二候选标签到底领先多少。

如果这个 margin 很小，说明虽然纠正侧票数表面上第一，但证据并不稳；这时强行替换全局预测，很容易误改 clean 样本。

因此，引入最小纠正门槛 (\tau_{\text{corr}})：

[
\Delta_{\text{corr}} \ge \tau_{\text{corr}}
]

只有满足这个条件，才允许执行纠正。

---

# 五、新增公式 2：纠正执行条件

将“是否执行纠正”定义为：

[
\mathbb I^{\text{correct}}
==========================

\mathbf 1\Big(
\Delta_{\text{corr}} \ge \tau_{\text{corr}}
\Big)
]

于是最终输出改为：

[
\hat y_{\text{final}}
=====================

\begin{cases}
c^*, & \text{if } \mathbb I^{\text{correct}}=1 \
\hat y_{\text{global}}, & \text{otherwise}
\end{cases}
]

这就是最核心的降 FPR 改动。

---

# 六、可选增强：加入“相对全局预测优势”门槛

如果你想更稳一点，还可以再加一层：

即使纠正侧内部有足够大 margin，也要要求它相对全局预测确实有明显优势。

由于你已经排除了与全局预测同向的客户端，可以定义全局预测在剩余客户端上的残余支持为：

[
\text{Score}*{\text{global}}=
\sum*{i\in\mathcal S_{\text{remain}}}\tilde{\alpha}*i \cdot \mathbf 1(v_i=\hat y*{\text{global}})
]

在当前设计下，这一项通常为 0，因为剩余集合本来就排除了与全局预测同向的客户端。
所以更实用的写法，是直接把它推广成“候选类对全局类的优势阈值”，例如：

[
\Delta_{\text{adv}} = \text{Score}(c^*) - \eta_{\text{global}}
]

其中 (\eta_{\text{global}}) 可以理解为一个小的全局保守阈值。
但从你现在的实现出发，这一层不是必需的。**先加 (\tau_{\text{corr}}) 就够了。**

---

# 七、推荐写进论文的完整版本

你可以把第三阶段纠正部分改写成下面这版：

---

### 3.5 联合加权投票与保守纠正机制

在得到剩余客户端集合 (\mathcal S_{\text{remain}}) 及其联合权重 (\tilde{\alpha}_i) 后，对每个候选类别 (c) 计算加权投票得分：

[
\text{Score}(c)=\sum_{i\in\mathcal S_{\text{remain}}}\tilde{\alpha}_i \cdot \mathbf 1(v_i=c)
]

记最高得分类别为：

[
c^{*}=\arg\max_c \text{Score}(c)
]

并记第一高得分与第二高得分分别为：

[
\text{Score}^{(1)}=\max_c \text{Score}(c),\qquad
\text{Score}^{(2)}=\max_{c\neq c^*}\text{Score}(c)
]

为避免对 clean 样本进行过于激进的误纠正，本文进一步定义纠正 margin：

[
\Delta_{\text{corr}}=\text{Score}^{(1)}-\text{Score}^{(2)}
]

其中，(\Delta_{\text{corr}}) 刻画了当前纠正候选类别相对于次优候选类别的证据优势程度。仅当该优势超过预设纠正门槛 (\tau_{\text{corr}}) 时，才执行纠正操作。故最终输出定义为：

[
\hat y_{\text{final}}
=====================

\begin{cases}
c^*, & \text{if } \Delta_{\text{corr}} \ge \tau_{\text{corr}} \
\hat y_{\text{global}}, & \text{otherwise}
\end{cases}
]

该机制的作用在于：即使样本已被检测为可疑，也不必然立即替换全局预测；只有当剩余客户端对某一纠正标签形成足够显著的一致性支持时，才执行标签修正。由此可有效降低 clean 样本上的误纠正概率，从而降低第三阶段的 false positive rate。

---

# 八、参数建议

## 1. 纠正门槛 (\tau_{\text{corr}})

这是新的关键参数。

推荐先扫：

[
\tau_{\text{corr}} \in {0.05, 0.10, 0.15, 0.20}
]

我的建议起点：

[
\tau_{\text{corr}} = 0.10
]

### 解释

* 太小：几乎 suspicious 就会改，FPR 降不下来
* 太大：很多 attack-success 样本虽然被检出，但不敢改，Recall/Correction rate 会掉

---

## 2. (\gamma)

为了避免权重过于 one-hot，建议一起扫更小值：

[
\gamma \in {0.5, 1.0, 1.5, 2.0}
]

当前如果日志里还经常出现 `[1.0, 0.0, 0.0]` 这种权重，我建议先从

[
\gamma = 1.0
]

开始。

---

## 3. (\theta_{\text{supp}})

为了减少 clean 样本误入 suspicious，建议试更低一档：

[
\theta_{\text{supp}} \in {0.05, 0.10, 0.15}
]

---

# 九、这版改进的核心逻辑

一句话概括：

**检测仍然负责“把可能有问题的样本筛出来”，纠正则改成“只有证据足够强才真正改标签”。**

这样第三阶段就从原来的：

[
\text{Suspicious} \Rightarrow \text{Correct}
]

变成：

[
\text{Suspicious} \Rightarrow \text{Evaluate Correction Confidence} \Rightarrow \text{Possibly Correct}
]

这正是降低 FPR 最有效的方向。

---

# 十、我最推荐你落地的最简版

如果你现在只想先做一个最小改动版本，我建议直接加这两条：

[
\Delta_{\text{corr}}=\text{Score}^{(1)}-\text{Score}^{(2)}
]

[
\hat y_{\text{final}}
=====================

\begin{cases}
c^*, & \text{if } \Delta_{\text{corr}} \ge \tau_{\text{corr}} \
\hat y_{\text{global}}, & \text{otherwise}
\end{cases}
]

然后参数先试：

* (\tau_{\text{corr}}=0.10)
* (\gamma=1.0)
* (\theta_{\text{supp}}=0.10)

---

如果你要，我下一条可以直接把这版 **“降低 FPR 的第三阶段改进公式”** 同步写进你现在的方案文档格式里。











