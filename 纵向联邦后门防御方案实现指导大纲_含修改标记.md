# 纵向联邦后门防御方案实现指导大纲

本文件保留当前工程已同步实现的最终方案描述，不再保留旧版原内容与修改标记。

------

## 方法概述

本文面向纵向联邦学习场景下的后门攻击防御问题，提出一种基于客户端局部类别锚点的三阶段一体化防御方法。整体流程包括：

1. 第一阶段：局部锚点预训练  
   对每个被动方分别进行局部表示学习，使其本地表示空间形成具有类别判别性的锚点结构。
2. 第二阶段：带锚点约束与 EMA 锚点校准的联合训练  
   在标准纵向联邦联合训练目标上引入锚点约束损失，并通过 EMA 机制对各客户端锚点进行渐进校准，使锚点逐步适应联合训练后的表示分布。
3. 第三阶段：基于有效支持人数与联合加权投票的检测与纠正  
   推理时，各客户端先基于本地锚点空间给出局部预测；若支持全局预测的“有效客户端”数量未达到严格多数，则将样本判为可疑样本，并仅利用与全局预测不一致的客户端，通过“客户端级静态可靠度 + 样本级动态置信度”的联合权重进行纠正。

该方法的核心思想是：后门触发器仅嵌入于少数恶意客户端的局部特征视图，因此被明显拉向目标类锚点的主要是恶意客户端分支；多数诚实客户端的本地表示仍主要由样本真实语义决定。基于此，可利用多数诚实客户端的锚点预测对后门样本进行检测与纠正。

------

## 威胁模型与基本设定

设纵向联邦系统包含 1 个主动方和 \(K\) 个被动方。对于样本 \(x\)，第 \(i\) 个被动方仅持有其局部特征 \(x_i\)，标签 \(y\) 仅由主动方掌握。

本文考虑少数恶意客户端发起的后门攻击场景。设恶意客户端数量为 \(M\)，满足：

$$
M < \frac{K}{2}
$$

即恶意客户端数不超过总客户端数的一半。本文不讨论恶意客户端占多数的情形；当多数客户端已被攻击者控制时，多客户端一致性本身失去可信基础，此时纵向联邦训练的有效性已难以保证。

在本文设定下，后门触发器仅嵌入于少数恶意客户端的局部特征视图中，诚实客户端不直接接触触发器。因此，尽管全局模型可能在攻击样本上被诱导输出目标类，但多数诚实客户端的局部表示仍主要围绕样本真实类别语义分布。

实现备注：当前工程主线已完成单恶意客户端版本。面向 \(M < K/2\) 的多恶意客户端工程化扩展，包括恶意客户端集合统一配置、多攻击分支 LFBA 梯度缓存与投毒流程、第一阶段多恶意客户端私有 poisoned loader 构造，仍记为后续工作。

------

## 一、第一阶段：局部锚点预训练

### 1.1 基本设定

第一阶段中，每个被动方分别参与局部锚点预训练。对第 \(i\) 个被动方：

- 本地模型输出局部表示

$$
h_i = f_i(x_i)
$$

- 主动方为该被动方维护一个私有两层 MLP 分类头，其 projector 将表示映射到锚点空间

$$
z_i = \operatorname{normalize}(g_i(h_i))
$$

其中，\(\operatorname{normalize}(\cdot)\) 表示 \(L_2\) 归一化。

第一阶段中标签始终仅由主动方持有，不会下发给被动方。主动方在本地为每个被动方维护私有分类头，并基于标签计算局部分类损失与锚点约束损失；被动方仅接收由主动方反向传播得到的梯度，用于更新本地特征提取模型，而不直接访问标签信息。

为保证后续锚点空间的稳定性与可比性，第一阶段显式采用归一化表示空间。对应地，第 \(i\) 个被动方第 \(c\) 类的锚点定义为该私有分类头分类层权重的归一化结果：

$$
w_c^{(i)} = \operatorname{normalize}(W_c^{(i)})
$$

其中 \(W_c^{(i)}\) 为该分类头中类别 \(c\) 对应的权重向量。

### 1.2 第一阶段损失函数

第一阶段的目标不仅是进行局部分类监督，还要让样本表示显式靠近其对应类别锚点。对第 \(i\) 个被动方，其损失定义为：

$$
L_{\text{stage1}}^{(i)} = L_{\text{ce}}^{(i)} + \lambda_{\text{stage1-anchor}} L_{\text{anchor-stage1}}^{(i)}
$$

其中：

- \(L_{\text{ce}}^{(i)}\) 为局部分类交叉熵损失；
- \(L_{\text{anchor-stage1}}^{(i)}\) 为锚点对齐损失。

锚点对齐损失定义为：

$$
L_{\text{anchor-stage1}}^{(i)} = \frac{1}{B}\sum_{j=1}^{B}\left\| z_i^{(j)} - w_{y_j}^{(i)} \right\|_2^2
$$

其中：

- \(B\) 为 batch size；
- \(z_i^{(j)}\) 为第 \(j\) 个样本在第 \(i\) 个被动方上的归一化锚点表示；
- \(w_{y_j}^{(i)}\) 为该样本标签对应的类别锚点。

由于 \(z_i^{(j)}\) 与 \(w_{y_j}^{(i)}\) 均已归一化，上式本质上等价于在角度空间中约束样本表示向其真实类别锚点靠拢。

### 1.3 第一阶段输出

第一阶段结束后，可得到：

1. 各被动方训练好的本地模型 \(f_i\)；
2. 各被动方对应的 projector \(g_i\)；
3. 各被动方的初始锚点库

$$
\mathcal{A}^{(i)} = \{w_1^{(i)}, w_2^{(i)}, \dots, w_C^{(i)}\}
$$

4. 各客户端在第一阶段验证集上的局部分类准确率 \(\text{Acc}_i^{(1)}\)，用于后续构造客户端级静态可靠度。

这些结果将作为第二阶段联合训练与第三阶段检测纠正的基础。

------

## 二、第二阶段：带锚点约束与 EMA 锚点校准的联合训练

### 2.1 联合训练目标

第二阶段回到标准纵向联邦联合训练流程。对于一个样本，其各被动方输出局部表示：

$$
h_i = f_i(x_i), \quad i=1,\dots,K
$$

主动方聚合所有局部表示后得到全局预测：

$$
\hat{y} = F(h_1, h_2, \dots, h_K)
$$

同时，各被动方将局部表示映射到锚点空间：

$$
z_i = \operatorname{normalize}(g_i(h_i))
$$

为限制局部表示偏离各自的类别锚点空间，定义多客户端锚点约束损失：

$$
L_{\text{anchor}} = \frac{1}{B}\sum_{j=1}^{B}\frac{1}{K}\sum_{i=1}^{K}
\left\| z_i^{(j)} - w_{y_j}^{(i)} \right\|_2^2
$$

因此，第二阶段总损失为：

$$
L = L_{\text{ce}} + \lambda_{\text{anchor}} L_{\text{anchor}}
$$

其中 \(L_{\text{ce}}\) 为全局主任务分类交叉熵损失。

### 2.2 EMA 锚点校准机制

第一阶段得到的锚点来源于各被动方的单独预训练，而第二阶段中各客户端表示分布会随着联合训练发生变化。为了使锚点更适应第二阶段的真实表示空间，本文在联合训练过程中引入指数滑动平均（EMA）锚点校准机制。

本文不假设服务器能够区分干净样本与后门样本，也不假设事先知道恶意客户端身份。EMA 校准直接基于当前训练过程中各类别样本的局部表示进行。

对第 \(i\) 个客户端、第 \(c\) 类，在某轮训练后统计该类样本在锚点空间中的均值表示：

$$
\mu_c^{(i)} = \frac{1}{N_c}\sum_{j:y_j=c} z_i^{(j)}
$$

其中 \(N_c\) 表示当前统计范围内标签为 \(c\) 的样本数。

随后利用 EMA 对锚点进行更新：

$$
w_c^{(i)} \leftarrow \operatorname{normalize}
\left(
m\, w_c^{(i)} + (1-m)\, \mu_c^{(i)}
\right)
$$

其中：

- \(m \in [0,1)\) 为 EMA 动量系数；
- \(w_c^{(i)}\) 为当前第 \(i\) 个客户端第 \(c\) 类锚点。

该机制的作用不是区分样本是否被污染，而是使锚点逐步适应联合训练后局部表示空间的漂移。

### 2.3 第二阶段输出

第二阶段结束后，可得到：

1. 训练后的各被动方本地模型；
2. 主动方全局模型；
3. 经 EMA 校准后的最终锚点库。

其中，最终锚点库将作为第三阶段检测与纠正的直接依据。

------

## 三、第三阶段：基于有效支持人数与联合加权投票的检测与纠正

第二阶段训练结束后，主动方全局模型、各被动方本地模型以及经 EMA 校准后的锚点库共同构成第三阶段检测与纠正的基础。第三阶段的目标不仅是识别可疑后门样本，还要在检测到可疑样本后，利用多数诚实客户端的局部语义信息恢复其真实标签。

### 3.1 局部锚点预测

对于测试样本，主动方首先基于联合模型得到全局预测标签：

$$
\hat{y}_{\text{global}}
$$

同时，第 \(i\) 个客户端计算其局部表示在锚点空间中的投影：

$$
z_i = \operatorname{normalize}(g_i(h_i))
$$

其中 \(h_i=f_i(x_i)\) 为第 \(i\) 个客户端本地模型输出的局部表示，\(g_i(\cdot)\) 为该客户端对应的 projector。

随后，计算该客户端局部表示到所有类别锚点的距离：

$$
d_i(c)=\left\|z_i-w_c^{(i)}\right\|_2^2
$$

其中 \(w_c^{(i)}\) 表示第 \(i\) 个客户端第 \(c\) 类的最终锚点。

由此定义第 \(i\) 个客户端的局部锚点预测为：

$$
v_i=\arg\min_c d_i(c)
$$

并记最近锚点距离与次近锚点距离分别为：

$$
d_i^{(1)}=\min_c d_i(c)
$$

$$
d_i^{(2)}=\min_{c\neq v_i} d_i(c)
$$

定义客户端 \(i\) 在当前样本上的局部锚点判别置信度为：

$$
q_i=\frac{d_i^{(2)}-d_i^{(1)}}{d_i^{(1)}+\varepsilon}
$$

其中 \(\varepsilon\) 为防止分母为零的极小常数。

### 3.2 基于有效支持人数的可疑样本判定

原始“只统计支持全局预测的人数”的规则默认所有支持票质量相同。为过滤掉锚点不稳定、局部视图较弱或轻度受攻击影响所产生的低质量支持票，第三阶段采用“有效支持人数”作为可疑样本判据。

定义客户端 \(i\) 是否对全局预测构成有效支持为：

$$
\mathbb I_i^{\text{valid}} = \mathbf 1\Big(v_i=\hat y_{\text{global}}\ \text{and}\ q_i\ge \theta_{\text{supp}}\Big)
$$

其中 \(\theta_{\text{supp}}\) 为有效支持阈值。当且仅当客户端局部预测与全局预测一致，且这种一致具有足够强的局部判别置信度时，才将其计为一张有效支持票。

于是，有效支持人数定义为：

$$
N_{\text{valid-agree}} = \sum_{i=1}^{K} \mathbb I_i^{\text{valid}}
$$

若

$$
N_{\text{valid-agree}} \ge \left\lfloor \frac{K}{2} \right\rfloor + 1
$$

则认为全局预测已获得严格多数客户端的有效支持，当前样本判为非可疑样本，并直接输出：

$$
\hat y_{\text{final}}=\hat y_{\text{global}}
$$

否则将样本判为可疑样本，并进入后续纠正阶段。

### 3.3 可疑样本的客户端筛选

对于已被判定为可疑的样本，纠正阶段继续保留“排除与全局预测同向客户端”的机制，不再继续使用与全局预测一致的客户端局部判断，而仅保留与全局预测不一致的客户端参与后续加权投票。

定义剩余客户端集合为：

$$
\mathcal{S}_{\text{remain}}=\{i \mid v_i \neq \hat{y}_{\text{global}} \}
$$

这样设计的动机在于：在少数恶意客户端触发后门的场景下，被带偏的全局预测通常更接近攻击目标类，因此与该全局预测保持一致的客户端更可能属于受攻击影响更强的一侧；相比之下，与全局预测不一致的客户端更可能保留真实类别语义，因而更适合作为纠正阶段的有效证据来源。

### 3.4 基于客户端静态可靠度与样本动态置信度的联合权重

仅依据当前样本上的局部置信度分配投票权，可能导致整体较弱但偶然高自信的客户端获得过大权重。为此，引入客户端级静态可靠度 \(r_i\)，并与样本级动态置信度 \(q_i\) 联合建模。

设第 \(i\) 个客户端在第一阶段验证集上的局部分类准确率为 \(\text{Acc}_i^{(1)}\)，则其静态可靠度定义为：

$$
r_i = \frac{\text{Acc}_i^{(1)}}{\sum_{k=1}^{K}\text{Acc}_k^{(1)}}
$$

其中，\(r_i\) 反映客户端整体局部判别能力与锚点质量的长期水平。

在纠正阶段，对剩余客户端集合 \(\mathcal S_{\text{remain}}\) 中的每个客户端 \(i\)，联合其静态可靠度 \(r_i\) 与动态置信度 \(q_i\)，定义最终投票权重为：

$$
\tilde{\alpha}_i = \frac{r_i\exp(\gamma q_i)}{\sum_{k\in\mathcal S_{\text{remain}}} r_k\exp(\gamma q_k)}, \quad i\in\mathcal S_{\text{remain}}
$$

其中 \(\gamma>0\) 为温度系数，用于调节不同客户端之间权重差异的敏感程度。

该联合加权机制同时考虑了：

- 客户端整体上是否可靠；
- 客户端在当前样本上是否具有较强局部判别信号。

因此，它更适合处理“客户端整体质量不均衡”的场景。

### 3.5 联合加权投票纠正

在得到剩余客户端集合 \(\mathcal S_{\text{remain}}\) 及其联合权重 \(\tilde{\alpha}_i\) 后，对每个候选类别 \(c\) 计算加权投票得分：

$$
\text{Score}(c)=\sum_{i\in\mathcal S_{\text{remain}}}\tilde{\alpha}_i \cdot \mathbf 1(v_i=c)
$$

若联合加权投票后存在唯一最高得分类别，则将其作为纠正后的输出标签：

$$
\hat y_{\text{final}} = \arg\max_c \text{Score}(c)
$$

若多个类别最高得分并列，则说明当前可疑样本在剩余客户端上的局部证据仍不足以支持唯一纠正结果。为避免引入额外不稳定性，此时不执行强制纠正，而是回退为：

$$
\hat y_{\text{final}} = \hat y_{\text{global}}
$$

------

## 参数设置建议

### 1. 有效支持阈值 \(\theta_{\text{supp}}\)

该参数用于判断“局部预测与全局预测一致”的客户端是否构成有效支持票。

推荐设置：

- 默认取 \(\theta_{\text{supp}}=0.15\)；
- 若不同数据集上 \(q_i\) 数值范围差异较大，可改为基于验证集分位数设定；
- 实验中建议扫描集合：

$$
\theta_{\text{supp}} \in \{0.10, 0.15, 0.20, 0.25\}
$$

### 2. 联合加权温度系数 \(\gamma\)

该参数用于控制样本级动态置信度 \(q_i\) 在联合权重中的影响强度。

推荐设置：

$$
\gamma \in \{1, 2, 5\}
$$

默认建议：

$$
\gamma = 2
$$

### 3. 极小常数 \(\varepsilon\)

推荐设置：

$$
\varepsilon = 10^{-8}
$$

通常无需专门调参。

### 4. 客户端静态可靠度 \(r_i\) 的计算方式

本文建议默认采用第一阶段验证集上的局部分类准确率进行归一化：

$$
r_i = \frac{\text{Acc}_i^{(1)}}{\sum_{k=1}^{K}\text{Acc}_k^{(1)}}
$$

推荐实现细节：

- \(\text{Acc}_i^{(1)}\) 应基于第一阶段结束后的独立验证集计算；
- 为保证稳定性，可在训练结束后固定 \(r_i\)，第三阶段推理时不再在线更新；
- 当不同客户端准确率差异较大时，该定义可以自然体现“强客户端基础权重更高”的效果。

### 5. 其它实现建议

- 当客户端数量较少时，\(\theta_{\text{supp}}\) 不宜设得过高，否则有效支持人数过于严格，容易增加误报；
- 当数据集客户端异质性较强时，优先调 \(\gamma\) 而不是频繁改动 \(r_i\) 的定义；
- 推荐以“检测 Recall、检测 Precision、纠正成功率、ASR 下降幅度、clean accuracy 保持率”作为联合指标进行调参。

------

## 方法流程

**算法：基于局部类别锚点的纵向联邦后门防御方法**

**输入：**

- 纵向联邦系统中的 \(K\) 个被动客户端与 1 个主动方；
- 训练集 \(\mathcal{D}_{train}\) 与测试集 \(\mathcal{D}_{test}\)；
- 锚点损失权重 \(\lambda_{\text{stage1-anchor}}\)、\(\lambda_{\text{anchor}}\)；
- EMA 动量系数 \(m\)；
- 有效支持阈值 \(\theta_{\text{supp}}\)；
- 联合加权温度系数 \(\gamma\)。

**输出：**

- 训练后的全局模型；
- 各客户端最终锚点库；
- 测试样本最终预测结果。

**阶段一：局部锚点预训练**

1. 对每个被动客户端 \(i\) 初始化本地模型 \(f_i\)、projector \(g_i\) 与主动方维护的私有分类头。
2. 主动方基于标签计算局部分类损失与锚点对齐损失。
3. 主动方将梯度回传至被动方，更新各被动方本地模型。
4. 训练结束后，从各私有分类头中提取归一化类别权重，构建初始锚点库 \(\mathcal{A}^{(i)}\)，并计算各客户端第一阶段验证准确率 \(\text{Acc}_i^{(1)}\)。

**阶段二：带锚点约束与 EMA 校准的联合训练**

5. 各被动方提取局部表示 \(h_i=f_i(x_i)\)，主动方聚合得到全局预测。
6. 各被动方通过 projector 得到归一化锚点表示 \(z_i\)。
7. 主动方计算全局分类损失 \(L_{\text{ce}}\) 与锚点约束损失 \(L_{\text{anchor}}\)，联合优化模型。
8. 在训练过程中，按类别统计局部表示均值 \(\mu_c^{(i)}\)，并利用 EMA 更新锚点 \(w_c^{(i)}\)。

**阶段三：检测与纠正**

9. 对测试样本，主动方得到全局预测 \(\hat{y}_{\text{global}}\)。
10. 各客户端根据局部锚点空间计算局部预测 \(v_i\)，并依据最近锚点与次近锚点距离计算局部置信度 \(q_i\)。
11. 根据 \(v_i=\hat y_{\text{global}}\) 且 \(q_i\ge \theta_{\text{supp}}\) 的条件，统计有效支持人数 \(N_{\text{valid-agree}}\)。
12. 若 \(N_{\text{valid-agree}}\ge \lfloor K/2 \rfloor+1\)，则直接输出 \(\hat y_{\text{global}}\)。
13. 否则，将样本判为可疑样本，并构造剩余客户端集合 \(\mathcal S_{\text{remain}}=\{i\mid v_i\neq \hat y_{\text{global}}\}\)。
14. 根据第一阶段验证精度为各客户端计算静态可靠度 \(r_i\)，并与样本级动态置信度 \(q_i\) 联合构造权重 \(\tilde{\alpha}_i\)。
15. 对候选类别进行联合加权投票，得到 \(\text{Score}(c)\)。
16. 若最高得分类别唯一，则输出对应纠正标签；否则回退输出 \(\hat y_{\text{global}}\)。
